{
  "name": "feature-card-full",
  "summary": "Run a feature card end to end: intake, context, Codex implementation, verification, Lens proof, summaries, ledger, and handoff.",
  "runner": "bash",
  "script": "workflows/feature-card-full.sh",
  "args": [
    {
      "name": "card_id",
      "required": true,
      "description": "Card identifier, for example CARD-123."
    },
    {
      "name": "task_file",
      "required": true,
      "description": "Markdown file containing the card details."
    },
    {
      "name": "repo_dir",
      "required": false,
      "description": "Target repository. Defaults to the current directory."
    }
  ],
  "quiet_by_default": true,
  "safety": {
    "require_git_repo": true,
    "allow_dirty_tree": true,
    "requires_network": false,
    "human_approval_recommended": true
  }
}
