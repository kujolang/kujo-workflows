# Todo

- [x] Select content pillar: agent-run DocsGen over a user-chosen repo.
- [x] Use `kujo docgen` directly with configurable target, language, output, and gate flags.
- [x] Include a small fixture repo so the workflow runs without external setup.
- [x] Capture generated docs, CLI JSON, stderr, command metadata, status, and handoff artifacts.
- [x] Add README and HOWTO.
- [ ] Optional: add sample command profiles for WordPress plugins, TypeScript packages, and Kujo repos.
- [ ] Optional: add a CI wrapper that uploads only summary artifacts while keeping generated docs local.
