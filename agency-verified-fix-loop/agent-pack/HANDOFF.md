# Handoff

## Current State

Static demo pack prepared for the Agency Verified Fix Loop.

## Operator Notes

The automation script applies the deterministic fix from `fixtures/fixed-overrides/` so the loop can run without model credentials.
