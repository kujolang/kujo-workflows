# Phase 02: Apply Scoped Fix

Apply the deterministic JS/CSS changes:

- Use form submit handling for the promo flow.
- Ensure first tap applies the promo code.
- Add mobile spacing around the sticky checkout bar.
